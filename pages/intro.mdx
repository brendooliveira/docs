---

title: Comece aqui
icon: 'share'
description: "Veja como é simples integrar com a gente 🚀"
----------------------------------------------------------

# Bem-vindo à API Cpay

Aqui você encontra tudo o que precisa para integrar de forma rápida, simples e segura.

---

## <Icon icon="Share" type="solid"/> Quem somos

A **Cpay** nasceu da nossa necessidade de simplificar cobranças.
Percebemos que muitos gateways do mercado são cheios de barreiras:

* Documentações extensas e pouco objetivas
* Diversas formas diferentes de executar a mesma operação
* Até exigência de treinamentos para conseguir integrar

Com a Cpay, eliminamos essa complexidade. Nossa proposta é transformar a burocracia dos meios de pagamento em uma API clara e intuitiva.

### Exemplo rápido

```php
<?php

use Cpay\Client\Client;

$payment = (new Client($_ENV('CPAY_API')))
    ->charge()
    ->create(
        amount: 200,
        name: 'Brendo Oliveira', // opcional
        document: '123.456.789-01' // opcional
    );
```

---

## <Icon icon="laptop" type="solid"/> API descomplicada

Nossa API segue dois princípios:

1. **Idempotência**
   A mesma requisição pode ser executada várias vezes sem efeitos colaterais.

   ```php
   $payment->charge()->create(...);
   ```

2. **Consistência**
   Todas as respostas seguem um padrão único: erro ou dados de retorno.
   Isso facilita a integração em linguagens fortemente tipadas e simplifica o tratamento de respostas.

   ```json
   {
       "error": null,
       "status": 200,
       "data": {
           "id": "ch_a6cccdcd-2ce0-4d24-a8a2-09f159144186",
           "amount": 300,
           "status": "PENDING",
           "customer": {
               "id": "cus_67d2b131-0a84-4e82-bab0-1f9b2fd63751",
               "name": "Brendo Oliveira",
               "document": "1234567801"
           }
       }
   }
   ```

Além disso, oferecemos **SDKs oficiais** e suporte a **modo de desenvolvimento** com chaves dedicadas.

---

## <Icon icon="cloud" type="solid"/> Suporte

Ficou com dúvidas? Nossa equipe está pronta para ajudar:

<Icon icon="envelope" type="solid"/> [contato@cpay.app.br](mailto:contato@cpay.app.br)
